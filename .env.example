# SQLite database file (relative to project root or absolute path)
DB_PATH=database/jagiree.sqlite

# Optional Apify fallback (admin Settings → Integrations is preferred)
# APIFY_API_TOKEN=
# APIFY_LINKEDIN_ACTOR_ID=
